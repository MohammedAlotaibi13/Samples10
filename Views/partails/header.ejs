  <head>
    <link rel="stylesheet" type="text/css" href="/stylesheets/header.css">
  </head>
   <nav class="navbar navbar-expand-lg navbar-light bg-light">
   <div class="container">
    <a class="navbar-brand" href="/">Samples</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/"> الرئيسية <span class="sr-only">(current)</span></a>
        </li>
         <% if(!currentUser) { %>
        <li class="nav-item">
          <a class="nav-link" href="/">أختبار تجريبي</a>
        </li>
        <% } else { %> 
        <a class="nav-link" href="/test/<%= currentUser.username %>">أختبار تجريبي</a>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="#price">ألاسعار</a>
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if(!currentUser) { %>
       <li class="nav-item">
          <a class="nav-link" href="/signIn">تسجيل الدخول</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/register">حساب جديد</a>
         </li>
         <% } else { %> 
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> <%= currentUser.username %></a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="/profile/<%= currentUser.id %>">الحساب</a>
                <a class="dropdown-item" href="/logout">خروج</a>
          </div>
         </li>
          <% if(currentUser.memberShip == "free" || currentUser.memberShip == "pro") { %>
         <li class="nav-item">
           <a href="/pay" class="nav-link  btn btn-outline-success">ترقية العضوية </a>
           <% } %>
         </li>
          <% } %>
      </ul>
   </div>
</nav>


<div class="container">
  <% if(error && error.length > 0) { %> 
  <div class="alert alert-danger" role="alert">
    <%= error %>
  </div>
  <% } %>
  <% if(success && success.length > 0) { %>
  <div class="alert alert-success text-center">
    <%= success %>
  </div>
  <% } %>
</div>